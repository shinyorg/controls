// The same namespace URI the rest of the suite uses, so a page that already has the shiny xmlns gets
// DiagramView without a second prefix.
[assembly: Microsoft.Maui.Controls.XmlnsDefinition("http://shiny.net/maui/controls", "Shiny.Maui.Controls.Diagram")]
